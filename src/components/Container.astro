


<div class="container">
  <slot />
</div>

<style>
  .container {
    align-items: center;
    padding-inline: 60px;
    @media (width < 1200px) {
      & {
        padding-inline: clamp(1.5rem, 10vw, 3rem);
      }
    }
  }
  div.main {
    align-items: center;
    padding-inline: 0px;
    @media (width < 1200px) {
      & {
        padding-inline: 20px;
      }
    }
  }
</style>
