---
---

<div class="pill">
  <slot />
</div>


<style>
  .pill {
    background-color: var(--clr-accent);
    color: var(--clr-darkest);
    padding: 0.5em 1em;
    border-radius: var(--border-radius);
  }
</style>